[project]
name = "lost-and-found"
version = "0.1.0"
description = "A simple Flask lost and found app for classroom teaching."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
]
